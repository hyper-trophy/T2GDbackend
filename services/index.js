const 
    {collabController} = require('./collab'),
    {googleSignIn} = require('./signIn'),
    {searchTorrents, getMagnet} = require('./torrent')

module.exports = {
    collabController,
    googleSignIn,
    searchTorrents,
    getMagnet
}
